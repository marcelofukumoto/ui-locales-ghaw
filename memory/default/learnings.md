# PT-BR Learnings

All 4 placeholder violations fixed (2026-03-16):
1. `drivers.deactivate.warningDrivers`: `{names}` restored
2. `istio.customOverlayFile.tip`: `{vendor}` + 2nd link restored
3. `monitoring.prometheus.warningInstalled`: 2nd paragraph + `{vendor}` restored
4. `monitoringReceiver.webhook.modifyNamespace`: full URL with HTML entities restored

Coverage: 100%. ~502 strings kept in English (brand/tech terms).
Key count must match en-us.yaml (not line count — block scalars differ).
